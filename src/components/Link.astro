---
export interface Props {
  alt?: string
  link: string
  newTab?: boolean
  className?: string
}

const { alt, link, newTab, className } = Astro.props
---

<a href={link} alt={alt} target={newTab ? "_blank" : "_self"} class={className} rel="noreferrer"
  ><slot /></a
>
