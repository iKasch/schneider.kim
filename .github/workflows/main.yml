name: Deployment

on:
  push:
    branches: 
      - master

jobs:
  build & deploy:
    name: build & deploy
    runs-on: ubuntu-latest

    steps:
      - name: checkout@master
      - uses: actions/checkout@v1
      - name: deploy with rsync
      - run: |
          rsync
